var PATHS = {
    FILES: {// All the paths should be relative to the web root folder. i.e. /server/
        DOWNLOADS: 'files/downloads/',
        LOGS: 'logs/',
        TEMP: 'files/temp/',
        UPLOADS_COMPRESSED: 'files/uploads/compressed/',
        UPLOADS_EXTRACTED: 'files/uploads/extracted/',
        UPLOADS_IMG: 'files/uploads/image/'
    }
};

var APP_CONFIG = {
    PATHS: PATHS
};

module.exports = APP_CONFIG;